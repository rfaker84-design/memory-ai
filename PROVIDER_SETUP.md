# ?? MemoryAI ? Provider ????

## ??

1. [????](#????)
2. [??????](#??????)
3. [Supabase ??](#supabase-??)
4. [DeepSeek AI ??](#deepseek-ai-??)
5. [??? COS + TTS ??](#???-cos--tts-??)
6. [???? Provider](#????-provider)
7. [??? Provider](#???-provider)
8. [GPU Worker ??](#gpu-worker-??)
9. [????](#????)
10. [????](#????)

---

## ????

```
                   Next.js App
  +----------+  +----------+  +---------------+
  | Memory   |  | AI Chat  |  | Voice Chat    |
  | CRUD     |  | DeepSeek |  | STT -> TTS    |
  +----------+  +----------+  +---------------+
  +----------+  +----------+  +---------------+
  | Voice    |  | Avatar   |  | Training      |
  | Clone    |  | Provider |  | Center        |
  +----+-----+  +----+-----+  +---------------+
       |              |
  +----v----+    +----v----+
  | CosyVoice|    | MiniMax |
  | Worker   |    | Avatar  |
  | (GPU)    |    | (Cloud) |
  +---------+    +---------+
```

---

## ??????

### ??????????????????

| ?? | ?? | ?? |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase | ?? API ?? |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase | ??????? |
| `DEEPSEEK_API_KEY` | AI | DeepSeek API ?? |
| `TENCENT_SECRET_ID` | ??? | API ?? ID |
| `TENCENT_SECRET_KEY` | ??? | API ?? Key |
| `COS_BUCKET` | ??? | COS ????? |
| `COS_REGION` | ??? | COS ?? |

### ????

| ?? | ?? | ?? |
|---|---|---|
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase | ??????????????? |
| `NEXT_PUBLIC_APP_URL` | ?? | ???????? |

### ?????????????

| ?? | ?? | ?? |
|---|---|---|
| `VOICE_CLONE_PROVIDER` | ???? | ?? provider?manual_v1 / cosyvoice / gpt_sovits |
| `COSYVOICE_WORKER_URL` | ???? | CosyVoice GPU Worker ?? |
| `GPT_SOVITS_WORKER_URL` | ???? | GPT-SoVITS Worker ?? |
| `AVATAR_PROVIDER` | ??? | ?? provider?adapter_v1 / minimax_avatar / tencent_zhiying |
| `MINIMAX_API_KEY` | ??? | MiniMax API ?? |
| `MINIMAX_GROUP_ID` | ??? | MiniMax ?? ID |
| `TENCENT_TTS_REGION` | TTS | ????????? ap-guangzhou? |
| `COS_DOMAIN` | COS | ??????? |

---

## Supabase ??

### 1. ????

?? [supabase.com/dashboard](https://supabase.com/dashboard)???????

### 2. ????

?? Dashboard ? Settings ? API?

- **Project URL** ? `NEXT_PUBLIC_SUPABASE_URL`
- **anon public** ? `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- **service_role secret** ? `SUPABASE_SERVICE_ROLE_KEY`????????

### 3. ???????

? Supabase SQL Editor ??? `supabase/migrations/` ???? SQL ???

---

## DeepSeek AI ??

### 1. ??

?? [platform.deepseek.com](https://platform.deepseek.com) ?????

### 2. ?? API Key

??? ? API Keys ? ????? ? ??? `DEEPSEEK_API_KEY`

### 3. ??

DeepSeek ???????????????

---

## ??? COS + TTS ??

### 1. ?? API ??

[??????](https://console.cloud.tencent.com/cam/capi) ? ???? ? API ?????

- **SecretId** ? `TENCENT_SECRET_ID`
- **SecretKey** ? `TENCENT_SECRET_KEY`

### 2. ?? COS ???

[???????](https://console.cloud.tencent.com/cos) ? ??????

- ?? ? `COS_BUCKET`????`bucket-name-appid`?
- ?? ? `COS_REGION`?? `ap-guangzhou`?
- ?????**??????**

### 3. ?????CORS?

??? ? ???? ? ???? CORS ???

```
?? Origin: *
?? Methods: GET, POST, PUT, HEAD
Allow-Headers: *
Expose-Headers: ETag
```

### 4. ?? TTS ??

[???????](https://console.cloud.tencent.com/tts) ? ?????

????????? TENCENT_SECRET_ID / TENCENT_SECRET_KEY?

---

## ???? Provider

### ?????manual_v1?

?????????????????????????????????????????

### CosyVoice??????

**??**???? CosyVoice GPU Worker?? [GPU Worker ??](#gpu-worker-??)??

? `.env.local` ????

```bash
VOICE_CLONE_PROVIDER=cosyvoice
COSYVOICE_WORKER_URL=http://your-gpu-server:8081
```

**??**?

1. ???????? ? COS
2. ??????????
3. Next.js ?? Worker `POST /jobs`
4. Worker ???? ? CosyVoice ?? ? ????? COS
5. Worker ?? `/api/voice-clone-callback`
6. ??????????

### GPT-SoVITS??????

```bash
VOICE_CLONE_PROVIDER=gpt_sovits
GPT_SOVITS_WORKER_URL=http://your-gpu-server:8082
```

???????????? API ???

---

## ??? Provider

### ?????adapter_v1?

????????????????????

### MiniMax Avatar

**??**???? [MiniMax ??](https://platform.minimaxi.com)?

```bash
AVATAR_PROVIDER=minimax_avatar
MINIMAX_API_KEY=your_api_key
MINIMAX_GROUP_ID=your_group_id
```

**??**?

1. ?????? ? COS
2. ???????????
3. Next.js ?? MiniMax Talking Avatar API
4. MiniMax ???? ? ?? `/api/avatar-callback`
5. ????????????

### ????

```bash
AVATAR_PROVIDER=tencent_zhiying
```

????? TENCENT_SECRET_ID / TENCENT_SECRET_KEY????????

---

## GPU Worker ??

### ????

- Ubuntu 22.04+
- NVIDIA GPU??? 16GB+ ???
- Python 3.10+
- CUDA 12.4+

### ????

```bash
# 1. ?? worker ??? GPU ???
scp -r worker/ user@gpu-server:/opt/memoryai-worker/

# 2. SSH ???????
ssh user@gpu-server
cd /opt/memoryai-worker
bash setup.sh

# 3. ??????
cp .env.example .env
# ?? .env ?????

# 4. ????
source venv/bin/activate
python server.py
```

### Worker ????

```bash
# ????
PORT=8081

# COS ????????????
COS_BUCKET=your-bucket-1234567890
COS_REGION=ap-guangzhou
COS_SECRET_ID=your_secret_id
COS_SECRET_KEY=your_secret_key

# Next.js ????
CALLBACK_BASE_URL=https://yijianmemory.cn

# ??????????
VOICE_SAMPLE_DIR=/tmp/voice-samples
OUTPUT_DIR=/tmp/voice-models
```

### Docker ??

```bash
docker build -t memoryai-worker .
docker run -d \
  --gpus all \
  -p 8081:8081 \
  -e COS_BUCKET=xxx \
  -e COS_REGION=ap-guangzhou \
  -e COS_SECRET_ID=xxx \
  -e COS_SECRET_KEY=xxx \
  -e CALLBACK_BASE_URL=https://yijianmemory.cn \
  memoryai-worker
```

---

## ????

### 1. ????

```bash
git clone <repo-url>
cd MemoryAi
```

### 2. ????

```bash
npm install
```

### 3. ??????

```bash
cp .env.example .env.local
```

?? `.env.local`??? Supabase?DeepSeek??????????

??????????????

```bash
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxx
DEEPSEEK_API_KEY=sk-xxx
TENCENT_SECRET_ID=AKIDxxx
TENCENT_SECRET_KEY=xxx
COS_BUCKET=xxx
COS_REGION=ap-guangzhou
```

### 4. ???????

? Supabase SQL Editor ?? `supabase/migrations/` ???? SQL ???

### 5. ???????

```bash
npm run dev
```

?? [http://localhost:3000](http://localhost:3000)?

---

## ????

### 1. ??

```bash
npm run build
```

### 2. ????

???? `.env.local` ?????

```bash
NEXT_PUBLIC_APP_URL=https://yijianmemory.cn
SUPABASE_SERVICE_ROLE_KEY=sb_secret_xxx
```

### 3. ??

```bash
# PM2
pm2 start npm --name memoryai -- start
pm2 save

# ???
npm start
```

### 4. Nginx ????

```nginx
server {
    listen 80;
    server_name yijianmemory.cn;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name yijianmemory.cn;

    ssl_certificate /etc/nginx/ssl/yijianmemory.cn.pem;
    ssl_certificate_key /etc/nginx/ssl/yijianmemory.cn.key;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    client_max_body_size 50m;
}
```

### 5. ???? Provider

???????????

```bash
# ????
VOICE_CLONE_PROVIDER=cosyvoice
COSYVOICE_WORKER_URL=http://gpu-server:8081

# ???
AVATAR_PROVIDER=minimax_avatar
MINIMAX_API_KEY=xxx
MINIMAX_GROUP_ID=xxx
```

---

## ????

### Q: ??????????

?? VOICE_CLONE_PROVIDER ?????Worker ??????? manual_v1 ?????????????

### Q: ??????????????

???????????????????????????

### Q: ??????????

???????????????ASR ????? 16kHz ?????

### Q: Worker ?????

????????? 8081 ???Worker ??????????

```bash
# ?? Worker ????
curl http://gpu-server:8081/health
```
