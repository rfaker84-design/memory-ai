import { redirect } from "next/navigation";

/**
 * The historical reveal screen made revival and autonomous-presence claims.
 * It is deliberately unavailable until a separately reviewed surface exists.
 */
export default function LegacyAvatarPresencePage() {
  redirect("/");
}
