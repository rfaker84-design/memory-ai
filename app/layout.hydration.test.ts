// The executable DOM hydration regression uses JSX and therefore lives in
// layout.hydration.test.tsx. This tracked compatibility file preserves the
// original test path for repository-wide tracked-file security scans.
export {};
